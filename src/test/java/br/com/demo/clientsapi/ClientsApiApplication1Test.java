/*package br.com.demo.clientsapi;

import java.lang.Class;
import java.lang.String;

import org.junit.Ignore;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.runners.MockitoJUnitRunner;
import org.springframework.boot.SpringApplication;

import static org.mockito.Mockito.*;
*/
/**
 * Test de la clase ClientsApiApplication
 */
/*@RunWith(MockitoJUnitRunner.class)
public class ClientsApiApplication1Test {
  @InjectMocks
  private ClientsApiApplication clientsApiApplication = new ClientsApiApplication();

  @Mock
  private SpringApplication springApplication;

  @Test(expected = IllegalArgumentException.class)
  public void testMain1() {
    when(springApplication).run(any(Class.class), any(String[].class));

    String[] string = new String[]{ "hyn9vlv4gpotf" };
    clientsApiApplication.main(string);
  }
}
*/